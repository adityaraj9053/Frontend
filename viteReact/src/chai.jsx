function Chai(){
    return(
        <>
            <h1>Learnings</h1>
            <p>1. React components must return a single parent element. example here, if we remove "greater than" symbol, it will show error    /
               2. function name must be in "capital letter".
            </p>
        </>
    )
}

export default Chai;